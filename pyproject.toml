[project]
name = "plane-mcp-selfhosted"
version = "0.1.0"
description = "Custom MCP server for self-hosted Plane"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
plane-mcp-selfhosted = "server:mcp.run"
